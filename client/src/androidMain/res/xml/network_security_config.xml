<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) to any host — this is a self-hosted game server,
         players connect to their own LAN IP, localhost, or emulator alias -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
